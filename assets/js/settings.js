/**
 * DressCode Tool settings page js.
 *
 * The original template's color-picker / image-upload demo fields have been
 * removed; this file is kept as a minimal hook for future settings-page
 * interactivity.
 *
 * @package DressCode Tool/Settings
 */

jQuery( document ).ready( function ( $ ) {
	// Intentionally empty for now.
} );
